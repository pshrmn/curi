import { RouteDescriptor, Route } from '../route';
import { Addon } from '../interface';
export default function walkRoutes(routeArray: Array<RouteDescriptor>, addons: Array<Addon>): Array<Route>;
