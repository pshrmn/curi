{
  "name": "@curi/route-ancestors",
  "version": "1.0.0-beta.10",
  "description": "Get a Curi route's ancestor routes",
  "main": "dist/curi-route-ancestors.js",
  "module": "dist/curi-route-ancestors.es.js",
  "types": "types/index.d.ts",
  "files": [
    "dist",
    "types",
    "LICENSE",
    "*.md"
  ],
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "node ./scripts/build",
    "coverage": "jest --coverage",
    "prepare": "npm run build",
    "prepublishOnly": "npm test",
    "prettier": "prettier --single-quote --write \"{src,tests}/**/*.ts\"",
    "test": "jest && TEST_ENV=cjs jest && TEST_ENV=umd jest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pshrmn/curi/tree/master/packages/interactions/route-ancestors"
  },
  "keywords": [
    "Curi",
    "route",
    "interaction",
    "ancestor",
    "parent"
  ],
  "author": "Paul Sherman",
  "license": "MIT",
  "dependencies": {
    "@curi/router": "^1.0.0-beta.43",
    "rollup-plugin-size-snapshot": "^0.6.1"
  },
  "devDependencies": {
    "@types/jest": "^22.0.1",
    "@types/node": "^8.0.53",
    "jest": "^22.1.1",
    "prettier": "^1.10.2",
    "rimraf": "^2.6.2",
    "rollup": "^0.65.0",
    "rollup-plugin-typescript2": "^0.14.0",
    "ts-jest": "^21.2.3",
    "typescript": "^3.0.1"
  }
}
